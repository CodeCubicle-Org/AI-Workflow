# Gemini CLI — project context

@AGENTS.md

## Optional cross-tool filename

To align with other tools that read `AGENTS.md` only, you can add `AGENTS.md` to `context.fileName` in Gemini CLI `settings.json` (see Gemini CLI documentation). This repository already includes root `AGENTS.md`.
